"""
Account routes.

Empty for Phase 1 — registration, profile and password endpoints land in
Phase 2. The module exists now so config/urls.py can include it without
churn later.
"""
from django.urls import path

app_name = "accounts"

urlpatterns: list[path] = []
